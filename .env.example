# Copy to .env (never commit .env). Restart the dev server after editing.
# Optional: lets the proxy add your World Labs key so you don't paste it in the app.
WORLDLABS_API_KEY=
# Needed for AI room labeling when running locally.
ANTHROPIC_API_KEY=
